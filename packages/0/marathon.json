{
  "id": "{{riak.framework-id}}",
  "instances": 1,
  "cpus": {{riak.cpus}},
  "mem": {{riak.mem}},
  "ports": [0, 0],
  "uris": ["{{riak.url}}"],
  "env": {},
  "args": [
      "riak_mesos_framework/framework_linux_amd64",
      "-master={{mesos.master}}",
      "-zk={{riak.zk}}",
      "-id={{riak.id}}"
      "-ip={{riak.ip}}",
      "-hostname={{riak.hostname}}",
      "-log={{riak.log}}",
      "-user={{riak.user}}",
      "-name=\"{{riak.name}}\"",
      "-role={{riak.role}}"],
  "healthChecks": []
}
